page {
  background-color: #000;
  color: #fff;
  line-height: 1;

  box-sizing: border-box;
  padding: 0 var(--padding-size);

  --padding-size: 20rpx;
  --padding-size-2: calc(var(--padding-size) * 1.5);
  --content-bg: #111;
  --spacer-height: 1.2rem;
}
